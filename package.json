{
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.13",
    "typescript": "7.0.2",
    "vite": "^8.3.0"
  },
  "scripts": {
    "dev": "vite src",
    "build": "tsc --noEmit && vite build src --outDir ../dist",
    "preview": "vite preview dist"
  }
}