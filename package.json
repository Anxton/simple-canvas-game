{
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.10",
    "typescript": "5.9.2"
  },
  "scripts": {
    "prebuild": "rm -rf dist && mkdir dist && cp -f ./src/index.html ./src/styles.css ./dist",
    "build": "tsc",
    "dev": "npm run prebuild && tsc --watch"
  }
}